@extends('layouts.scaffold')

@section('main')

<h1>Edit {{Model}}</h1>
{{ Form::model(${{model}}, array('method' => 'PATCH', 'class'=>'form-horizontal', 'route' => array('{{models}}.update', ${{model}}->id))) }}
{{formElements}}
		<div class="form-group">
			{{ Form::submit('Update', array('class' => 'btn btn-info')) }}
			{{ link_to_route('{{models}}.show', 'Cancel', ${{model}}->id, array('class' => 'btn')) }}
		</div>
{{ Form::close() }}


@stop
